[pytest]
testpaths = tests

addopts =
    --cov=src
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=80
    --tb=short
    -ra

python_files = test_*.py
filterwarnings =
    ignore::DeprecationWarning
